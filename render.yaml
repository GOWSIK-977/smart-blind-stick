services:
  - type: web
    name: smart-blind-stick
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python blind_sticks.py
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://gowsik977_db_user:gowsik123@cluster1.t4w8mul.mongodb.net/?appName=Cluster1
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: RENDER
        value: true
    plan: free
    healthCheckPath: /health
